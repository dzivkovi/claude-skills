# skill-creator eval runner output — machine-specific, reproducible, noisy.
# Teaching artifacts (trigger-eval.json, eval_review.html, workspace README)
# are committed; the raw run dump is not.
branded-docx-workspace/run-results/
read-along-workspace/run-results/
print/
work/
branded-pptx-build/
.playwright-mcp/

# Python bytecode (e.g. from running scripts/setup/font_preflight.py)
__pycache__/
*.pyc
